# GENAPP Reverse Engineering Deliverables

The Word document `genapp_reverse_engineering.docx` is stored in this repository for convenience. If your environment blocks binary downloads, use the base64-encoded variant instead:

```
base64 --decode genapp_reverse_engineering.docx.b64 > genapp_reverse_engineering.docx
```

This will reconstruct the Word document locally.
